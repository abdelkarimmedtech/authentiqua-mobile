# Authentiqua

Authentiqua is an Expo React Native app (AI-powered image authenticity scanner).

Quick start:

1. Install dependencies: `npm install` or `yarn`
2. Start: `npx expo start` or `yarn start`

This repo includes a modular structure under `src/` with navigation, services, context and screens.
